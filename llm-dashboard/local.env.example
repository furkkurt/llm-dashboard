# Copy to `local.env` (same folder as `backend/`). That file is gitignored and is not read or
# written by setup.sh. Values here override `.env` for the same variable name.
#
#   cp local.env.example local.env
#
# Put API keys here so overwriting `.env` from a template does not remove them.

OPENROUTER_API_KEY=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
